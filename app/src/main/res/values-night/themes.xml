<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Noteon" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>
    <!-- themes.xml -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- Set the splash screen background color -->
        <item name="windowSplashScreenBackground">@color/colorBackground</item>

        <!-- Use the Noteon logo as splash screen icon -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/noteon_logo</item>

        <!-- Set the theme used after splash screen is shown -->
        <item name="postSplashScreenTheme">@style/Theme.Noteon</item>
    </style>
</resources>